root@localhost
12345

to enable ur http://localhost:8000/ and postman:
php -S localhost:8000 -t public

http://localhost/sia_site1/

uncheck password
exceed username's length

User.php
public $timestamps = false;

UserController.php
return response()->json($users, 200); -> return response()->json(['data' => $users], 200);
return $this->successResponse($user,Response::HTTP_CREATED); -> return response()->json($user, 200);

More Database Operations>>Empty Database
Import>>Execute SQL Script


